---
import TicketStamp from "../../components/ticket-stamp";
import Layout from "../../layouts/Layout.astro";

const { user_id } = Astro.params;
---

<Layout>
  <TicketStamp user_id = {user_id} client:load />
</Layout>
